{
  "name": "spotify-upgrade-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd telegram-bot && npm install && cd ../admin-dashboard && npm install",
    "start": "node start.js"
  }
}
